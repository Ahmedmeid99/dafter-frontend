
import classes from "./NotFind.module.css"
const NotFind = () => {
    return (
        <div className={ `${classes.notfind} container` }>
            <h3>
                قيد التطوير
            </h3>
            <p>not completed yet</p>
        </div>
    );
}

export default NotFind;
